# 環境構築手順書（雛形）

## 1. 前提条件
- Dockerインストール済み
- 必要なリソース（リポジトリ、環境変数等）

## 2. セットアップ手順
1. リポジトリをクローン
2. `docker compose up -d` で起動
3. `docker compose exec app bash -c "cd src && composer install && npm install"`
4. `.env` 設定
5. マイグレーション・シーディング

## 3. 動作確認
- ローカルでアクセスし、画面表示を確認

<!-- 詳細は環境に合わせて追記 -->
